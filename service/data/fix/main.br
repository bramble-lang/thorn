fn my_main() -> i64 {
    let x:i64 := 5 * -7;
    let a:[i64; 1] := [x];
    let ms: MyStruct := MyStruct{f: a[0]};
    project::std::io::writei64ln(ms.f);

    let b:bool := root::second::my_bool();
    project::std::io::writeboolln(b);
    return 0;
}

struct MyStruct {
    f: i64,
}