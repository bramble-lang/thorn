fn my_main() -> i64 {
    let x:i64 := 5 * -7;
    let a:[MyStruct; 1] := [MyStruct{f: 1}];
    project::std::io::writei64ln(a[0].f);

    let b:i64 := root::second::my_bool() as i64;
    project::std::io::writei64ln(b);
    return 0;
}

struct MyStruct {
    f: i64,
}